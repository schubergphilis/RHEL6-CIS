---
molecule:
  ignore_paths:
    - lib
    - bin
    - library
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: centos6
      image: centos
      image_version: centos6
      privileged: True
      ansible_groups:
        - docker
verifier:
  name: goss
